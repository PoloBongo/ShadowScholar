using UnityEngine;

public class Mission1 : IMission
{

    public Mission1(int _number, string name, string _objectif, string _localization_path_texture) : base(_number, name, _objectif, _localization_path_texture)
    {
    }

    public void LaunchMission()
    {

    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
