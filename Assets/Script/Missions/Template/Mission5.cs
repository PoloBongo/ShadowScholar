using UnityEngine;

public class Mission5 : IMission
{
    public Mission5(int _number, string name, string _objectif, string _localization_path_texture) : base(_number, name, _objectif, _localization_path_texture)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
